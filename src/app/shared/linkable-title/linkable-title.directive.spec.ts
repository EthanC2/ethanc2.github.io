import { LinkableTitleDirective } from './linkable-title.directive';

describe('LinkableTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkableTitleDirective();
    expect(directive).toBeTruthy();
  });
});
